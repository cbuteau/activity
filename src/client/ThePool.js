

class ThePool {
    constructor() {
    }

    pickup(options) {

    }

    dropoff(options) {
    }
}

let instance;
let inst = !instance ? instance = new ActivityDispatcher() : instance;

export default inst;