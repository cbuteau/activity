

class ThePool {
    constructor() {
    }

    pickup(options) {

    }

    dropoff(options) {
    }
}

let instance;
let inst = !instance ? instance = new ThePool() : instance;

export default inst;